/*arima_hw_version_bygpio() - Use gpio get hw version
* @vadc:	Structure device for qpnp vadc
* @hw_version: hard ware version by gpio
*/

int arima_hw_detection(unsigned int *buf , int *gpio_array , int gpio_size);
